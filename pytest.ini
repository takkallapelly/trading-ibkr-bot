[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Custom markers
markers =
    slow: marks tests that hit the network or run long backtests (deselect with -m "not slow")

# Coverage
addopts =
    --tb=short
    -q
